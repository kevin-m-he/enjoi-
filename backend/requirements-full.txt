# Optional quality upgrades — install on capable machines (GPU recommended).
# Each maps to a capability flag in /api/health; the app falls back gracefully
# without them (see docs/API_CONTRACT.md rule 2).
-r requirements-core.txt

# Music generation (MusicGen). Needs torch; CUDA build recommended:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchaudio>=2.1
audiocraft>=1.3.0

# Lyric transcription with word timestamps
faster-whisper>=1.0

# Stem separation for instrumentation profiling
demucs>=4.0.1

# High-accuracy f0 for autotune/vibrato analysis
torchcrepe>=0.0.22

# Formant-safe stretch (also requires the `rubberband` CLI on PATH)
pyrubberband>=0.4.0

# NOTE: madmom and essentia are intentionally not listed — they have no reliable
# Windows wheels for Python 3.11. The analysis module uses librosa equivalents
# and will automatically use madmom/essentia if you manage to install them.
